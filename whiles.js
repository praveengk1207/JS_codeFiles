let index = 0;
while (index <= 10) {
  console.log(index);
  index = index + 2;
}

let myarr = ["sa", "an", "he"];

let arr = 0;
while (arr < myarr.length) {
  console.log(myarr[arr]);
  arr = arr + 1;
}

let score = 20;

do {
  console.log(score);
  score++;
} while (score <= 30);
{
  console.log(score);
}
